# OSIV False + ResponseDTO + JOIN FETCH 학습 정리

> **강의 진행 시 빠른 파악을 위한 핵심 내용 정리**

---

## 📋 목차

1. [Open Session in View (OSIV) False 설정](#1-open-session-in-view-osiv-false-설정)
2. [ResponseDTO 설계](#2-responsedto-설계)
3. [JOIN FETCH 사용](#3-join-fetch-사용)
4. [list.mustache에 username 추가](#4-listmustache에-username-추가)
5. [전체 흐름 정리](#5-전체-흐름-정리)

---

## 1. Open Session in View (OSIV) False 설정

### 1.1 설정 위치

**파일**: `src/main/resources/application-dev.yml`

```yaml
spring:
  jpa:
    open-in-view: false  # OSIV를 false로 설정
```

### 1.2 OSIV란?

**Open Session in View (OSIV)**: Hibernate의 세션을 HTTP 요청 전체 기간 동안 유지하는 패턴

| 설정 | 동작 방식 | 장단점 |
|------|----------|--------|
| **OSIV = true** (기본값) | 뷰 렌더링까지 세션 유지<br>→ LAZY 로딩 가능 | ✅ 편리함<br>❌ 예상치 못한 쿼리 발생<br>❌ 트랜잭션 범위가 불명확 |
| **OSIV = false** | 트랜잭션 종료 시 세션 종료<br>→ LAZY 로딩 불가 | ✅ 트랜잭션 범위 명확<br>✅ 예상치 못한 쿼리 방지<br>❌ Service에서 명시적 조회 필요 |

### 1.3 OSIV False일 때 문제점

```java
// ❌ 문제 발생 코드
@Transactional(readOnly = true)
public List<Board> 게시글목록조회() {
    List<Board> boardList = boardRepository.findAll();
    // 트랜잭션 종료 → 세션 종료
    return boardList;  // LAZY 로딩된 user는 사용 불가!
}

// Controller에서
List<Board> boards = boardService.게시글목록조회();
// 템플릿에서 board.getUser().getUsername() 호출 시
// → LazyInitializationException 발생!
```

### 1.4 해결 방법

**Service에서 필요한 데이터를 모두 조회하고 DTO로 변환하여 반환**

```java
// ✅ 올바른 코드
@Transactional(readOnly = true)
public List<BoardResponse.ListDTO> 게시글목록조회() {
    // JOIN FETCH로 user를 함께 조회
    List<Board> boardList = boardRepository.findAllWithUserOrderByCreatedAtDesc();
    // 트랜잭션 내에서 DTO로 변환
    return boardList.stream()
            .map(BoardResponse.ListDTO::new)
            .collect(Collectors.toList());
}
```

---

## 2. ResponseDTO 설계

### 2.1 DTO를 사용하는 이유

| 이유 | 설명 |
|------|------|
| **계층 간 결합도 감소** | 엔티티를 직접 반환하지 않아 Controller와 Entity 간 결합도 감소 |
| **필요한 데이터만 전달** | 엔티티의 모든 필드가 아닌 필요한 필드만 전달 |
| **OSIV False 대응** | 트랜잭션 내에서 필요한 데이터를 모두 조회하여 DTO로 변환 |
| **보안** | 민감한 정보(비밀번호 등)를 제외하고 전달 가능 |

### 2.2 ResponseDTO 구조

**파일**: `src/main/java/org/example/demo_ssr_v1_1/board/BoardResponse.java`

```java
public class BoardResponse {
    
    /**
     * 게시글 목록 응답 DTO
     */
    @Data
    public static class ListDTO {
        private Long id;
        private String title;
        private String username;  // 작성자명 (평탄화)
        private String createdAt; // 포맷된 생성일

        public ListDTO(Board board) {
            this.id = board.getId();
            this.title = board.getTitle();
            // JOIN FETCH로 이미 로딩된 user 사용
            if (board.getUser() != null) {
                this.username = board.getUser().getUsername();
            }
            // 날짜 포맷팅
            if (board.getCreatedAt() != null) {
                this.createdAt = MyDateUtil.timestampFormat(board.getCreatedAt());
            }
        }
    }
    
    /**
     * 게시글 상세 응답 DTO
     */
    @Data
    public static class DetailDTO {
        private Long id;
        private String title;
        private String content;
        private Long userId;      // 작성자 ID
        private String username;  // 작성자명
        private String createdAt; // 포맷된 생성일

        public DetailDTO(Board board) {
            this.id = board.getId();
            this.title = board.getTitle();
            this.content = board.getContent();
            // JOIN FETCH로 이미 로딩된 user 사용
            if (board.getUser() != null) {
                this.userId = board.getUser().getId();
                this.username = board.getUser().getUsername();
            }
            if (board.getCreatedAt() != null) {
                this.createdAt = MyDateUtil.timestampFormat(board.getCreatedAt());
            }
        }
    }
}
```

### 2.3 DTO 설계 원칙

1. **평탄화 (Flattening)**: 중첩된 객체를 평면적으로 변환
   ```java
   // ❌ 중첩 구조 (템플릿에서 접근 복잡)
   board.user.username
   
   // ✅ 평탄화 (템플릿에서 접근 간단)
   board.username
   ```

2. **생성자에서 변환**: 엔티티를 받아 DTO로 변환하는 로직을 생성자에 구현

3. **null 안전성**: null 체크를 통해 안전하게 데이터 추출

---

## 3. JOIN FETCH 사용

### 3.1 JOIN FETCH란?

**JOIN FETCH**: 연관된 엔티티를 한 번의 쿼리로 함께 조회하는 JPA 기능

### 3.2 N+1 문제

**N+1 문제**: 연관된 엔티티를 조회할 때 발생하는 성능 문제

```java
// ❌ N+1 문제 발생
List<Board> boards = boardRepository.findAll();
// 1번의 쿼리: SELECT * FROM board_tb

for (Board board : boards) {
    board.getUser().getUsername();  
    // N번의 쿼리: SELECT * FROM user_tb WHERE id = ?
    // 총 1 + N번의 쿼리 발생!
}
```

### 3.3 JOIN FETCH로 해결

**파일**: `src/main/java/org/example/demo_ssr_v1_1/board/BoardRepository.java`

```java
/**
 * 게시글 전체 조회 (작성자 정보 포함)
 */
@Query("SELECT b FROM Board b JOIN FETCH b.user ORDER BY b.createdAt DESC")
List<Board> findAllWithUserOrderByCreatedAtDesc();

/**
 * 게시글 ID로 조회 (작성자 정보 포함)
 */
@Query("SELECT b FROM Board b JOIN FETCH b.user WHERE b.id = :id")
Optional<Board> findByIdWithUser(@Param("id") Long id);
```

**생성되는 SQL**:
```sql
-- 한 번의 쿼리로 Board와 User를 함께 조회
SELECT b.*, u.* 
FROM board_tb b 
INNER JOIN user_tb u ON b.user_id = u.id 
ORDER BY b.created_at DESC
```

### 3.4 Service에서 사용

**파일**: `src/main/java/org/example/demo_ssr_v1_1/board/BoardService.java`

```java
@Transactional(readOnly = true)
public List<BoardResponse.ListDTO> 게시글목록조회() {
    // JOIN FETCH로 Board와 User를 한 번의 쿼리로 조회
    List<Board> boardList = boardRepository.findAllWithUserOrderByCreatedAtDesc();
    
    // 엔티티를 DTO로 변환하여 반환
    return boardList.stream()
            .map(BoardResponse.ListDTO::new)
            .collect(Collectors.toList());
}

@Transactional(readOnly = true)
public BoardResponse.DetailDTO 게시글상세조회(Long id) {
    // JOIN FETCH로 Board와 User를 한 번의 쿼리로 조회
    Board board = boardRepository.findByIdWithUser(id)
            .orElseThrow(() -> new Exception404("게시글을 찾을 수 없어요 : "));
    
    // 엔티티를 DTO로 변환하여 반환
    return new BoardResponse.DetailDTO(board);
}
```

### 3.5 JOIN FETCH 장점

| 장점 | 설명 |
|------|------|
| **성능 최적화** | 한 번의 쿼리로 필요한 데이터 모두 조회 |
| **N+1 문제 해결** | 각 엔티티마다 추가 쿼리가 발생하지 않음 |
| **OSIV False 대응** | 트랜잭션 내에서 필요한 데이터를 모두 조회 |

### 3.6 주의사항

1. **명시적 LAZY 로딩 트리거 불필요**
   ```java
   // ❌ 불필요한 코드 (JOIN FETCH 사용 시)
   if (board.getUser() != null) {
       board.getUser().getUsername(); // LAZY 로딩 트리거
   }
   
   // ✅ JOIN FETCH 사용 시 user가 이미 로딩되어 있음
   // DTO 생성자에서 바로 사용 가능
   ```

2. **DTO 생성자에서 null 체크는 유지**
   ```java
   // ✅ 방어적 프로그래밍을 위한 null 체크는 유지
   if (board.getUser() != null) {
       this.username = board.getUser().getUsername();
   }
   ```

---

## 4. list.mustache에 username 추가

### 4.1 변경 전

**파일**: `src/main/resources/templates/board/list.mustache`

```mustache
{{#boardList}}
<div class="card mb-3">
    <div class="card-body">
        <h4 class="card-title mb-3">{{title}}</h4>
        <a href="/board/{{id}}" class="btn btn-primary">상세보기</a>
    </div>
</div>
{{/boardList}}
```

### 4.2 변경 후

```mustache
{{#boardList}}
<div class="card mb-3">
    <div class="card-body">
        <div class="d-flex justify-content-between align-items-center mb-2">
            <h4 class="card-title mb-0">{{title}}</h4>
            <small class="text-muted">작성자: {{username}}</small>
        </div>
        <a href="/board/{{id}}" class="btn btn-primary">상세보기</a>
    </div>
</div>
{{/boardList}}
```

### 4.3 변경 사항

- **작성자 정보 표시**: 제목 옆에 작성자명(`{{username}}`) 추가
- **레이아웃 개선**: `d-flex`를 사용하여 제목과 작성자를 양쪽 정렬

### 4.4 데이터 흐름

```
1. Repository: JOIN FETCH로 Board + User 조회
   ↓
2. Service: Board 엔티티를 ListDTO로 변환
   ↓
3. Controller: ListDTO 리스트를 Model에 추가
   ↓
4. Template: {{username}}으로 작성자명 표시
```

---

## 5. 전체 흐름 정리

### 5.1 게시글 목록 조회 흐름

```
[Controller]
  ↓
[Service] @Transactional(readOnly = true)
  ├─ Repository.findAllWithUserOrderByCreatedAtDesc()
  │   └─ JOIN FETCH로 Board + User 한 번에 조회
  │
  └─ Stream으로 Board → ListDTO 변환
      └─ DTO 생성자에서 user.username 추출
  ↓
[Controller] Model에 ListDTO 리스트 추가
  ↓
[Template] {{username}}으로 작성자명 표시
```

### 5.2 핵심 포인트

1. **OSIV False 설정**
   - `application-dev.yml`에 `open-in-view: false` 설정
   - 트랜잭션 종료 시 세션 종료

2. **ResponseDTO 설계**
   - Service에서 엔티티를 DTO로 변환하여 반환
   - 평탄화된 구조로 템플릿에서 접근 용이

3. **JOIN FETCH 사용**
   - Repository에 `@Query`로 JOIN FETCH 메서드 정의
   - Service에서 JOIN FETCH 메서드 사용
   - N+1 문제 해결 및 성능 최적화

4. **템플릿 수정**
   - DTO의 평탄화된 필드(`username`) 사용
   - 중첩 구조(`user.username`) 대신 평면 구조(`username`) 사용

### 5.3 코드 변경 요약

| 파일 | 변경 내용 |
|------|----------|
| `application-dev.yml` | `open-in-view: false` 추가 |
| `BoardResponse.java` | ResponseDTO 클래스 생성 (ListDTO, DetailDTO) |
| `BoardRepository.java` | JOIN FETCH 메서드 추가 (`findAllWithUserOrderByCreatedAtDesc`, `findByIdWithUser`) |
| `BoardService.java` | JOIN FETCH 메서드 사용, DTO 반환으로 변경 |
| `BoardController.java` | DTO 타입으로 변경 |
| `list.mustache` | `{{username}}` 추가 |

---

## 6. 학습 체크리스트

### 6.1 개념 이해

- [ ] OSIV False의 의미와 필요성
- [ ] ResponseDTO를 사용하는 이유
- [ ] JOIN FETCH의 동작 원리
- [ ] N+1 문제와 해결 방법

### 6.2 실습

- [ ] OSIV False 설정
- [ ] ResponseDTO 클래스 생성
- [ ] JOIN FETCH 메서드 작성
- [ ] Service에서 DTO 반환
- [ ] 템플릿에서 DTO 필드 사용

### 6.3 주의사항

- [ ] JOIN FETCH 사용 시 명시적 LAZY 로딩 트리거 불필요
- [ ] DTO 생성자에서 null 체크 유지
- [ ] 트랜잭션 내에서 DTO 변환 완료

---

## 7. 참고 자료

### 7.1 관련 파일

- `src/main/resources/application-dev.yml` - OSIV 설정
- `src/main/java/org/example/demo_ssr_v1_1/board/BoardResponse.java` - ResponseDTO
- `src/main/java/org/example/demo_ssr_v1_1/board/BoardRepository.java` - JOIN FETCH 메서드
- `src/main/java/org/example/demo_ssr_v1_1/board/BoardService.java` - Service 로직
- `src/main/resources/templates/board/list.mustache` - 템플릿

### 7.2 핵심 키워드

- **OSIV (Open Session in View)**
- **ResponseDTO (Data Transfer Object)**
- **JOIN FETCH**
- **N+1 Problem**
- **LAZY Loading**
- **Transaction Boundary**

---

**작성일**: 2026년  
**프로젝트**: Spring Blog (SSR v1.1)  
**학습 목표**: OSIV False 환경에서 ResponseDTO와 JOIN FETCH를 활용한 효율적인 데이터 조회

